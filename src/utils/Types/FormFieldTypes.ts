import Checkboxes from "../../components/Form/Checkboxes/Checkboxes";

export enum FormFieldTypesInterface {
    select = 'select',
    input = 'input',
    checkbox = 'checkbox',
    checkboxes = 'checkboxes',
    tabs = 'tabs'
}

export const formFieldTypes = {
    // checkboxes: () => <Checkboxes title='sss' data={[]} />
}